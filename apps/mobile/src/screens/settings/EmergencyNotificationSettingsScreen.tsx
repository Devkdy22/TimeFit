import { SettingsScreen } from './SettingsScreen';

/** Route-compatible wrapper for the consolidated notification settings. */
export function EmergencyNotificationSettingsScreen() {
  return <SettingsScreen />;
}
